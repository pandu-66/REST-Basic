<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit</title>
</head>
<body>
    <h1>Edit Post</h1>
    <p>Post Id: <%= post.id %></p>
    <p>Username: <%= post.username %></p>
    <form method="post" action="/posts/<%= post.id %>?_method=PATCH">
        <textarea name="content" cols="35" rows="10"><%= post.content %></textarea>
        <button>Submit</button>
    </form>
</body>
</html>